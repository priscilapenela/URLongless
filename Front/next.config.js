//next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  reactStrictMode: true,
  // ... otras configuraciones
};

module.exports = nextConfig; // <-- Asegúrate de que termine así
